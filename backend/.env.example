# === Configuration ProNett ===

# Port du serveur (défaut: 3001)
PORT=3001

# Mot de passe admin pour le tableau de bord
ADMIN_PASSWORD=Admin2024!

# Email de l'administrateur (reçoit les notifications)
ADMIN_EMAIL=votre-email@example.com

# === Configuration email (choisir UNE option) ===

# Option 1 — Resend (recommandé pour Railway / hébergement cloud)
# Créez un compte gratuit sur resend.com, puis copiez votre API key ici
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# URL publique du site (utilisé dans le lien du dashboard dans les emails admin)
# SITE_URL=https://pronett-production.up.railway.app

# Option 2 — SMTP classique (Gmail, etc.)
# Sans configuration, un compte de test Ethereal est utilisé automatiquement
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=votre-email@gmail.com
# SMTP_PASS=votre-mot-de-passe-application
